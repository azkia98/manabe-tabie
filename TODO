Project:
    + create the validation for members.create section
    + create the backable data for our craete form
    + create the members.edit section
    + create the card layout section
    + add city and state property
    + add city and state to edit section
    + craete the excel export for our application
    + create the * Gates * for our application
    + create the index page for our application
    + create the signature for our application
    + create CURD for our users
    + create the background for our cards
    + create the options section
    + fix the excel export
    + add village
    + create the excel import section
    + add search or filter section for our application
    + create report section for our dashboard `MemberCounts,UserCounts,ProtectorCount,PromoterCount,StudentCount`
    - change some bugs same as `birth-date field,issue-date field`
    - fix form size  
    x change the way for showing publish date
    + Add remove ability for roles
    x Redirect index page to login
    + Add validation for image file
    - Create the sms panel
